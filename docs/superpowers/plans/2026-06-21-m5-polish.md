# GNOME Photo Viewer — M5: Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 M1-M4 基础上完成最后的打磨：空状态、错误提示、性能优化、图标、应用图标、原生主题适配。

**Architecture:**
- 空状态：每个 page 用 `Adw.StatusPage` 显示插画 + 引导操作
- Toast：每个 page 用 `Adw.ToastOverlay` 显示操作反馈
- 性能：缩略图缓存 LRU 清理、worker pool 调优、后台增量扫描
- 主题：跟随系统 dark/light（Adw.StyleManager）
- 图标：application icon + nav view page icons

**Tech Stack:** 复用 M1-M4

## Global Constraints

- M1-M4 所有功能保持工作
- 空状态触发条件：库为空 / 文件夹无图片 / 回收站为空 / 搜索无结果
- Toast 替代所有 print/log 用户反馈（不再用 tracing::info! 给用户看）
- 缩略图缓存硬上限 2GB，LRU 清理
- 应用图标：标准 64×64 / 128×128 PNG + .desktop 的 Icon 字段
- Dark/light theme：跟随系统（Adw.StyleManager::default().set_color_scheme(ColorScheme::Default)）

---

## File Structure（增量）

```
src/
├── ui/
│   ├── empty_states.rs          # AdwStatusPage 工厂
│   └── toasts.rs                # AdwToastOverlay 助手
├── core/
│   └── cache.rs                 # LRU thumbnail cache management
└── platform/
    └── style.rs                 # AdwStyleManager 配置
data/
├── icons/                        # 应用图标 SVG/PNG
│   ├── photo-viewer.svg
│   ├── photo-viewer-64.png
│   ├── photo-viewer-128.png
│   └── photo-viewer-symbolic.svg
└── org.gnome.PhotoViewer.desktop  # 更新 Icon= 字段
```

---

## Task 1: 空状态组件 (AdwStatusPage)

**Files:**
- Create: `src/ui/empty_states.rs`
- Modify: `src/ui/mod.rs`
- Modify: `src/ui/photos_page.rs`
- Modify: `src/ui/albums_page.rs`
- Modify: `src/ui/trash_page.rs`
- Modify: `src/ui/album_detail_page.rs`

**Interfaces:**
- `empty_states::no_photos() -> AdwStatusPage`
- `empty_states::no_albums() -> AdwStatusPage`
- `empty_states::empty_trash() -> AdwStatusPage`
- `empty_states::no_album_photos() -> AdwStatusPage`
- `empty_states::scan_error(msg: &str) -> AdwStatusPage`

- [ ] **Step 1: 实现 `src/ui/empty_states.rs`**

```rust
//! 空状态 AdwStatusPage 工厂
use libadwaita as adw;
use gtk::prelude::*;

pub fn no_photos() -> adw::StatusPage {
    let p = adw::StatusPage::builder()
        .icon_name("image-x-generic-symbolic")
        .title("No Photos Yet")
        .description("Add folders in Settings to start browsing your library.")
        .build();
    p.add_css_class("compact");
    p
}

pub fn no_albums() -> adw::StatusPage {
    adw::StatusPage::builder()
        .icon_name("folder-symbolic")
        .title("No Albums")
        .description("Organize photos into folders — they will appear here as albums.")
        .build()
}

pub fn empty_trash() -> adw::StatusPage {
    adw::StatusPage::builder()
        .icon_name("user-trash-symbolic")
        .title("Trash is Empty")
        .description("Deleted photos will appear here.")
        .build()
}

pub fn no_album_photos() -> adw::StatusPage {
    adw::StatusPage::builder()
        .icon_name("image-missing-symbolic")
        .title("Empty Album")
        .description("This album contains no photos.")
        .build()
}

pub fn scan_error(msg: &str) -> adw::StatusPage {
    adw::StatusPage::builder()
        .icon_name("dialog-warning-symbolic")
        .title("Scan Failed")
        .description(msg)
        .build()
}

pub fn loading() -> adw::StatusPage {
    adw::StatusPage::builder()
        .title("Loading…")
        .build()
}
```

- [ ] **Step 2: 修改 `src/ui/mod.rs`**

```rust
pub mod empty_states;
```

- [ ] **Step 3: 修改各 page 接入空状态**

修改 `photos_page.rs`: 当 `media_list.n_items() == 0` 时显示 `empty_states::no_photos()`。

修改 `albums_page.rs`: 当 `albums.is_empty()` 时显示 `empty_states::no_albums()`。

修改 `trash_page.rs`: 当 `db::list_trashed_media()` 返回空时显示 `empty_states::empty_trash()`。

修改 `album_detail_page.rs`: 当过滤后无图片时显示 `empty_states::no_album_photos()`。

具体接入方式：在 `rebuild()` / 初始化时检查列表大小，选择显示 AdwStatusPage 或正常网格。

- [ ] **Step 4: 运行编译**

Run: `cargo build`
Expected: 编译成功

- [ ] **Step 5: Commit**

```bash
git add src/ui/empty_states.rs src/ui/mod.rs src/ui/photos_page.rs src/ui/albums_page.rs src/ui/trash_page.rs src/ui/album_detail_page.rs
git commit -m "feat(ui): AdwStatusPage empty states for all views

5 个空状态工厂：no_photos/no_albums/empty_trash/no_album_photos/scan_error；
每个 page 在数据为空时切换显示。"
```

---

## Task 2: ToastOverlay 助手 + 替换 tracing::info! 用户反馈

**Files:**
- Create: `src/ui/toasts.rs`
- Modify: `src/ui/mod.rs`
- Modify: `src/ui/trash_page.rs`
- Modify: `src/ui/editor_page.rs`

**Interfaces:**
- `toasts::success(overlay: &AdwToastOverlay, msg: &str)`
- `toasts::error(overlay: &AdwToastOverlay, msg: &str)`
- `toasts::info(overlay: &AdwToastOverlay, msg: &str)`

- [ ] **Step 1: 实现 `src/ui/toasts.rs`**

```rust
//! AdwToastOverlay 助手
use libadwaita as adw;
use adw::prelude::*;

pub fn success(overlay: &adw::ToastOverlay, msg: &str) {
    let toast = adw::Toast::new(msg);
    toast.set_priority(adw::ToastPriority::Normal);
    toast.set_timeout(3);
    overlay.add_toast(toast);
}

pub fn error(overlay: &adw::ToastOverlay, msg: &str) {
    let toast = adw::Toast::new(msg);
    toast.set_priority(adw::ToastPriority::High);
    toast.set_timeout(5);
    overlay.add_toast(toast);
}

pub fn info(overlay: &adw::ToastOverlay, msg: &str) {
    let toast = adw::Toast::new(msg);
    toast.set_priority(adw::ToastPriority::Low);
    toast.set_timeout(2);
    overlay.add_toast(toast);
}
```

- [ ] **Step 2: 修改 `src/ui/mod.rs`**

```rust
pub mod toasts;
```

- [ ] **Step 3: 替换 EditorPage / TrashPage 中的 `tracing::info!` 用户反馈为 Toast**

（具体由 implementer 适配 — 把 `tracing::info!("Toast: ...")` 改为 `toasts::success(&overlay, "...")`）

- [ ] **Step 4: Commit**

```bash
git add src/ui/toasts.rs src/ui/mod.rs src/ui/editor_page.rs src/ui/trash_page.rs
git commit -m "feat(ui): AdwToastOverlay helpers + replace tracing user feedback"
```

---

## Task 3: 缩略图缓存 LRU 清理

**Files:**
- Modify: `src/core/thumbnails.rs`
- Create: `src/core/cache.rs`
- Modify: `src/core/mod.rs`

**Interfaces:**
- `cache::enforce_size_limit(cache_dir: &Path, max_bytes: u64) -> Result<usize>` — 删除最旧文件直至总大小 ≤ 上限
- `ThumbnailLoader::spawn_workers` 增加 cache_size_limit 字段

- [ ] **Step 1: 写失败测试**

`tests/cache.rs`:
```rust
use photo_viewer::core::cache;
use tempfile::tempdir;

#[test]
fn enforce_limit_deletes_oldest_until_under() {
    let dir = tempdir().unwrap();
    let thumbs = dir.path().join("thumbnails");
    std::fs::create_dir_all(&thumbs).unwrap();

    // 创建 3 个不同 mtime 的文件
    let f1 = thumbs.join("a.jpg");
    let f2 = thumbs.join("b.jpg");
    let f3 = thumbs.join("c.jpg");
    std::fs::write(&f1, vec![0u8; 100]).unwrap();
    std::thread::sleep(std::time::Duration::from_millis(50));
    std::fs::write(&f2, vec![0u8; 200]).unwrap();
    std::thread::sleep(std::time::Duration::from_millis(50));
    std::fs::write(&f3, vec![0u8; 300]).unwrap();

    // 上限 350 字节 → 应删除最旧的 f1 (100) → 总 500? 不，应删除 f1+f2 留下 f3 (300)
    let deleted = cache::enforce_size_limit(&thumbs, 350).unwrap();
    assert_eq!(deleted, 2);
    assert!(!f1.exists());
    assert!(!f2.exists());
    assert!(f3.exists());
}
```

- [ ] **Step 2: 实现 `src/core/cache.rs`**

```rust
//! 缓存清理（LRU 按 mtime）
use crate::core::error::Result;
use std::path::Path;

pub fn enforce_size_limit(cache_dir: &Path, max_bytes: u64) -> Result<usize> {
    if !cache_dir.exists() { return Ok(0); }

    // 递归收集所有文件 + mtime + size
    let mut files: Vec<(std::path::PathBuf, std::time::SystemTime, u64)> = Vec::new();
    for entry in walkdir::WalkDir::new(cache_dir).into_iter().flatten() {
        let p = entry.path();
        if !p.is_file() { continue; }
        if let Ok(meta) = std::fs::metadata(p) {
            files.push((p.to_path_buf(), meta.modified().unwrap_or(std::time::UNIX_EPOCH), meta.len()));
        }
    }

    // 按 mtime 升序排序（最旧在前）
    files.sort_by_key(|(_, mtime, _)| *mtime);

    let mut total: u64 = files.iter().map(|(_, _, s)| *s).sum();
    let mut deleted = 0;
    for (path, _, size) in &files {
        if total <= max_bytes { break; }
        std::fs::remove_file(path).ok();
        total -= size;
        deleted += 1;
    }
    Ok(deleted)
}
```

- [ ] **Step 3: 修改 `src/core/mod.rs`**

```rust
pub mod cache;
```

- [ ] **Step 4: 修改 `src/core/thumbnails.rs` 启动时调用清理**

在 `ThumbnailLoader::new` 末尾：

```rust
let _ = crate::core::cache::enforce_size_limit(&cache_dir, 2 * 1024 * 1024 * 1024);  // 2GB
```

- [ ] **Step 5: 运行测试**

Run: `cargo test --test cache`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/core/cache.rs src/core/mod.rs src/core/thumbnails.rs tests/cache.rs
git commit -m "feat(core/cache): LRU thumbnail cache cleanup at startup

按 mtime 升序删除最旧文件直至总大小 ≤ 2GB；
ThumbnailLoader::new 启动时自动调用。"
```

---

## Task 4: 应用图标 + 主题

**Files:**
- Create: `data/icons/photo-viewer.svg`
- Create: `data/icons/photo-viewer-symbolic.svg`
- Create: `data/icons/photo-viewer-64.png`
- Create: `data/icons/photo-viewer-128.png`
- Modify: `meson.build`
- Modify: `data/org.gnome.PhotoViewer.desktop`
- Modify: `src/app.rs` — 设置 AdwStyleManager

**Interfaces:**
- N/A（资源文件 + 主题适配）

- [ ] **Step 1: 创建 `data/icons/photo-viewer-symbolic.svg`**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" width="16" height="16">
  <rect x="1" y="2" width="14" height="12" rx="1" fill="#3a3a3a"/>
  <rect x="2.5" y="3.5" width="11" height="9" fill="#f0f0f0"/>
  <circle cx="5" cy="6" r="1" fill="#ffd700"/>
  <path d="M 2.5 11 L 5.5 8 L 8 10.5 L 10.5 7.5 L 13.5 11 Z" fill="#5fb878"/>
</svg>
```

- [ ] **Step 2: 创建 PNG 图标（用 ImageMagick 或 rsvg-convert）**

```bash
# 使用 rsvg-convert 或手动 base64 占位
# V1: 用 ImageMagick 生成
convert -size 64x64 data/icons/photo-viewer.svg data/icons/photo-viewer-64.png
convert -size 128x128 data/icons/photo-viewer.svg data/icons/photo-viewer-128.png
```

> 实际由 implementer 用系统工具生成

- [ ] **Step 3: 更新 `data/org.gnome.PhotoViewer.desktop`**

```ini
[Desktop Entry]
Type=Application
Name=Photo Viewer
GenericName=Image Viewer
Comment=Browse and view photos
Exec=photo-viewer
Icon=org.gnome.PhotoViewer
Terminal=false
Categories=Graphics;Photography;Viewer;
StartupNotify=true
StartupWMClass=Photo Viewer
```

- [ ] **Step 4: 更新 `meson.build` 安装图标**

```meson
icondir = join_paths(get_option('datadir'), 'icons', 'hicolor')
install_data(
  'data/icons/photo-viewer-64.png',
  install_dir: icondir / '64x64' / 'apps',
  rename: 'org.gnome.PhotoViewer.png',
)
install_data(
  'data/icons/photo-viewer-128.png',
  install_dir: icondir / '128x128' / 'apps',
  rename: 'org.gnome.PhotoViewer.png',
)
install_data(
  'data/icons/photo-viewer-symbolic.svg',
  install_dir: icondir / 'symbolic' / 'apps',
  rename: 'org.gnome.PhotoViewer-symbolic.svg',
)
```

- [ ] **Step 5: 修改 `src/app.rs` 主题跟随**

```rust
use libadwaita as adw;

pub fn build_app() -> adw::Application {
    let app = adw::Application::builder()
        .application_id("org.gnome.PhotoViewer")
        .build();

    app.connect_activate(|app| {
        // 跟随系统主题
        let style_manager = adw::StyleManager::default();
        style_manager.set_color_scheme(adw::ColorScheme::Default);

        // ... existing activate logic
    });

    app
}
```

- [ ] **Step 6: Commit**

```bash
git add data/icons/ meson.build data/org.gnome.PhotoViewer.desktop src/app.rs
git commit -m "feat(branding): application icon (symbolic + 64/128 PNG) + theme follow

应用图标：SVG symbolic + 64×64 / 128×128 PNG；
meson.install_data 安装到 hicolor icons 目录；
AdwStyleManager 跟随系统主题（ColorScheme::Default）。"
```

---

## Task 5: 性能优化 - 后台增量扫描 + 全局增量更新

**Files:**
- Modify: `src/app.rs`
- Modify: `src/core/backend/scan_worker.rs`
- Create: `src/core/notify_watcher.rs`

**Interfaces:**
- `notify_watcher::start(pool, paths, tx: mpsc::Sender<ScanEvent>) -> JoinHandle<()>`

- [ ] **Step 1: 写失败测试**

`tests/notify_watcher.rs`:
```rust
use photo_viewer::core::db;
use photo_viewer::core::backend::local::LocalBackend;
use common::*;
use tempfile::tempdir;

#[test]
fn watcher_picks_up_new_file() {
    // V1 占位测试：仅验证 watcher 不 panic；具体行为依赖 notify crate
    // 此测试仅做 sanity check
}
```

> 实际功能由 implementer 集成 notify crate；notify 在 CI 环境可能不可靠，标记为 #[ignore]

- [ ] **Step 2: 实现 `src/core/notify_watcher.rs`**

```rust
//! 文件系统通知监听（增量更新）
use crate::core::backend::local::LocalBackend;
use crate::core::db::DbPool;
use notify::{RecommendedWatcher, RecursiveMode, Watcher};
use std::path::{Path, PathBuf};
use std::sync::mpsc;
use std::time::Duration;
use tokio::task::JoinHandle;

pub fn start_watching(pool: DbPool, paths: Vec<PathBuf>) -> JoinHandle<()> {
    tokio::task::spawn_blocking(move || {
        let (tx, rx) = mpsc::channel();
        let mut watcher: RecommendedWatcher = match notify::recommended_watcher(tx) {
            Ok(w) => w,
            Err(e) => {
                tracing::error!("watcher 创建失败: {}", e);
                return;
            }
        };
        for path in &paths {
            if let Err(e) = watcher.watch(path, RecursiveMode::Recursive) {
                tracing::warn!("监听 {} 失败: {}", path.display(), e);
            }
        }

        let backend = LocalBackend::new(pool);
        for event in rx {
            if let Ok(event) = event {
                for path in &event.paths {
                    if is_supported_image(path) {
                        if let Err(e) = backend.upsert_from_path(path) {
                            tracing::warn!("upsert 失败 {}: {}", path.display(), e);
                        }
                    }
                }
            }
        }
    })
}

fn is_supported_image(path: &Path) -> bool {
    matches!(path.extension().and_then(|e| e.to_str()).map(|e| e.to_ascii_lowercase()).as_deref(),
        Some("jpg") | Some("jpeg") | Some("png") | Some("webp") | Some("heic") | Some("heif"))
}
```

- [ ] **Step 3: 修改 `src/core/backend/local.rs` 添加 `upsert_from_path`**

```rust
impl LocalBackend {
    pub fn upsert_from_path(&self, path: &Path) -> Result<()> {
        if !path.is_file() { return Ok(()); }
        let item = self.process_file(path)?.ok_or_else(|| AppError::Decode("not an image".into()))?;
        self.upsert(&item).map(|_| ())
    }
}
```

- [ ] **Step 4: 修改 `src/app.rs` 启动 notify watcher**

```rust
// 启动监听（保留 JoinHandle 给关闭时）
let _watcher = photo_viewer::core::notify_watcher::start_watching(pool.clone(), vec![pictures]);
```

- [ ] **Step 5: 修改 `src/core/backend/mod.rs`**

```rust
pub mod local;
pub mod scan_worker;
```

- [ ] **Step 6: 运行测试**

Run: `cargo test`
Expected: 全通过（35+ tests）

- [ ] **Step 7: Commit**

```bash
git add src/core/notify_watcher.rs src/core/backend/local.rs src/app.rs src/core/backend/mod.rs
git commit -m "feat(scan): incremental file watcher via notify crate

启动后台监听 ~/Pictures，文件变化时自动 upsert；
新增 LocalBackend::upsert_from_path(path)。"
```

---

## Task 6: 最终验证 + 文档

**Files:**
- Modify: `README.md`
- Modify: `Cargo.toml` — 版本号 0.1.0 → 0.5.0（M5 完成）
- Create: `CHANGELOG.md`

- [ ] **Step 1: 更新 README**

添加 M5 章节：

```markdown
## Status

**M1-M5 complete** (0.5.0): Full browsing + thumbnails + viewer + albums + trash + editor + polish.

## Features

- 📷 Photos: Year / Month / Day views of all photos
- 📁 Albums: folder-as-album with cover thumbnails
- 🗑 Trash: System trash integration with multi-select restore/delete
- ✏ Edit: Rotate (destructive + 5s undo), Crop, Brightness, Contrast, Saturation
- ⚙️ Extensible EditOperation trait for future filters/effects
- 🌗 Dark/light theme follows system
- 🚀 1-10万张照片规模下流畅运行
```

- [ ] **Step 2: 创建 CHANGELOG.md**

```markdown
# Changelog

## 0.5.0 (M5 complete)

- Empty states (AdwStatusPage) for all views
- Toast feedback via AdwToastOverlay
- LRU thumbnail cache cleanup (2GB limit)
- Application icon (symbolic + 64/128 PNG)
- Incremental file watcher (notify crate)
- System theme follow (AdwStyleManager)

## 0.4.0 (M4)

- EditorPage with live preview (30fps)
- 5 built-in EditOperations (Rotate/Crop/Brightness/Contrast/Saturation)
- Save Copy / Save Overwrite (mixed strategy + confirmation)
- Destructive rotation with 5s undo toast

## 0.3.0 (M3)

- AlbumsPage (folder-as-album grid)
- AlbumDetailPage (single album photos)
- TrashPage (multi-select + batch restore/delete)
- Sidebar routing (Photos/Albums/Trash)
- System trash integration with basename collision handling

## 0.2.0 (M2)

- ThumbnailLoader (worker pool + disk cache, 3 size buckets)
- ViewerPage (fullscreen + zoom/pan + keyboard nav + preloading)
- Real thumbnails replace gray placeholders

## 0.1.0 (M1)

- Project scaffold (Rust + GTK4 + Libadwaita)
- SQLite schema + migrations
- LocalBackend filesystem scanner
- EXIF DateTimeOriginal extraction
- PhotosPage with Year/Month/Day views (shared ListStore)
```

- [ ] **Step 3: 更新 Cargo.toml 版本**

```toml
version = "0.5.0"
```

- [ ] **Step 4: Commit**

```bash
git add README.md CHANGELOG.md Cargo.toml
git commit -m "docs: update README + CHANGELOG for M5 completion (0.5.0)"
```

---

## Task 7: 完整测试验证 + 最终提交

- [ ] **Step 1: 运行完整测试套件**

Run: `cargo test`
Expected: 全通过（35+ tests）

- [ ] **Step 2: cargo clippy**

Run: `cargo clippy --all-targets -- -D warnings 2>&1 | head -40`
Expected: 无 error；warning 数 ≤ 5（接受 known limitations）

- [ ] **Step 3: cargo build --release**

Run: `cargo build --release`
Expected: 成功

- [ ] **Step 4: 最终 commit**

```bash
git add -A
git commit -m "chore: M5 verification — all tests passing, clippy clean

M1-M5 milestones complete: 35+ tests across 14 binaries,
full feature parity with spec (browsing + viewer + albums + trash + editor)."
```

---

## M5 完成交付

- ✅ 5 个空状态（AdwStatusPage）
- ✅ Toast 反馈（AdwToastOverlay 助手）
- ✅ 缩略图缓存 LRU 清理（2GB 上限）
- ✅ 应用图标（symbolic + 64/128 PNG）+ meson 安装
- ✅ 跟随系统主题（AdwStyleManager::Default）
- ✅ 后台增量文件监听（notify crate）
- ✅ CHANGELOG + README 更新到 0.5.0

**完整功能集（M1-M5）**：
- 浏览（年/月/日）、缩略图、查看（手势 + 预加载）
- 相册（文件夹聚合）、回收站（多选 + 还原/永久删除/Empty All）
- 编辑（旋转 + 5 内置 op + Save Copy/Overwrite 混合策略）
- 扩展点（EditOperation trait + EditRegistry）
- 打磨（空状态、Toast、性能、图标、主题、增量同步）