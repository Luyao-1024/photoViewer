//! Core domain types and errors.
//!
//! Real core types (PhotoAsset, Album, library state) will be added in
//! subsequent tasks. The empty module exists so `pub mod core` in lib.rs
//! resolves cleanly during the M1 scaffold.

pub mod error;